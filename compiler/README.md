# compiler

UCL compiler. Consumes /constitution UCL. Emits: policy decisions (OPA-style), relationship tuples (OpenFGA-style), workflow constraints, model-checkable invariants, runtime grant formats, audit schemas. Build target for Phase 2. Never emits application behavior.
