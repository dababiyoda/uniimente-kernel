pyyaml>=6.0
pytest>=8.0
jsonschema>=4.20
# Reality Aperture trust root. Ed25519 signing and fail-closed verification.
# Declared because the previous consequence-gate work imported cryptography
# (and pydantic, and cffi) without declaring any of them, so its hostile suite
# could not run from a clean clone. A hostile suite that cannot execute is not
# protecting anything.
cryptography>=41.0
