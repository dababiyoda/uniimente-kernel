"""Cross-organ pathways: the mechanisms that already exist, composed into one runnable thing.

Final Build Order section 6 names eight bridges. Before this package, every part
of Bridge A existed and passed its own tests — the DALEOBANKS packet adapter, the
WealthMachine assessment adapter, the signed transport, the event spine, causal
memory — and `adapters/` was imported by exactly zero non-test modules. The
institution held a verified parts bin, not a pathway.

Nothing here is a new mechanism. Every line composes something already built and
already tested somewhere else.
"""
