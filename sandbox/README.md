# sandbox

Agent Cell sandbox: isolated workspace, typed tools, least capability, quotas, no ambient secrets, no cross-cell memory. Build target for Phase 8. Adversarial escape tests in tests/security must pass before any executable stage.
