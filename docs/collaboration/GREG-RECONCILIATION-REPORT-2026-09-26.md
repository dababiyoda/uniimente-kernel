# GREG reconciliation report — 2026-09-26 (OPUS MAXIMUS §78, 38 sections)

Scope: the OPUS MAXIMUS directive re-sent unchanged on 2026-09-26, executed against repository
truth as of that day. It builds on `GREG-BODY-DECISION-2026-09-25.md` §1–14, which holds the full
earlier coverage, tables and two-pass reviews. Nothing here is a claim of Mac verification,
founder use or external outcome unless stated.

Reality labels used below: PROPOSED · IMPLEMENTED · UNIT TESTED · INTEGRATION TESTED · SANDBOXED ·
LOCAL-REAL · MAC-VERIFIED · FOUNDER-USED · EXTERNALLY VERIFIED.

## 1. Source coverage manifest

| Source family | State | Founder intent / implementation extracted | Evidence tier |
|---|---|---|---|
| All 7 repositories, default heads re-fetched 2026-09-26 | inspected | kernel `04b1b7e`, DALEOBANKS `e2f1ebe`, WMI `ec82b80`, RailScout `017d9f3`, PumpStation `db6758d`, RESEARCH-IN `15ceb62` unchanged since 09-09/09-17 | git objects |
| Kernel open PRs (15 listed by update time) | inspected (titles, heads, dates); #112 fully diffed; #70's new commit read | #112 competes with #113; #70 adds the verified-deficit rule | git objects + PR bodies |
| DALEOBANKS open PRs (8), #79 new since 09-25 | #79 inspected; others by body | community correction gated by source plus held-out gain | PR body + commit |
| Kernel doctrine (Effect Compiler, Intent Ledger, Execution/Build Order, Collaboration Protocol) | inspected earlier; binding | INTENT-0030, the 09-11/09-22/09-25 intents, the Spider-Web rule | canonical docs |
| Prior-session coverage (all repos' code, tests, proof, AGENTS/CLAUDE files) | see `GREG-BODY-DECISION-2026-09-25.md` §1 and `REPOSITORY-METABOLISM-2026-09-25.json` | 46 components classified A–G | code + tests |
| ChatGPT project conversations, exports, PDFs and uploads outside the repositories | **unavailable in this environment** | not claimed | — |
| tailscale.com docs, igalia.com blog | **egress-blocked**; the facts were verified empirically instead (WebCrypto in Chromium 141) and via search results | secure-context requirement verified | executed probe |

This is not a claim to have read every historical artifact.

## 2. Complete founder intent reconstruction

A persistent, founder-sovereign artificial institution on Alfonso's own computer. GREG is its one
face. It holds signed persistent missions and keeps working with every interface closed. It uses
real capabilities (files, repositories, browser, OS automation, APIs), and acquires missing ones by
search, verification and attachment. It waits economically and escalates once per scope. It is
reachable from the phone. It reports every morning and accepts critique that becomes tests and
policy. Over time it runs businesses with AI and human workers and compounds knowledge, revenue and
compute. It never grows in sovereignty, and shutdown always wins. Optimize the transaction:
authorized intention → verified outcome → an easier next intention (Spider-Web rule).

## 3. Chronological founder correction map

09-09 INTENT-0030 effect-not-metaphor → 09-10 GREG-INTENT-1 one bounded persistent mission → 09-11
INTENT-GREG-EMBODIED (the computer as body, phone as remote, night shift and morning tribunal, SOPs,
humans) → 09-22 INTENT-2026-09-22-FIRST-BODY → 09-25 INTENT-2026-09-25-EGREGORE-ECOLOGY (Levin
ecology, community, continuity without a survival right, build the effects for real) → 09-25
INTENT-2026-09-25-SPIDER-WEB-COMPOUNDING → 09-26 directive re-sent: **no new intent**, execution
continues.

## 4. Current reality vs destination (§81)

| Product step | Status |
|---|---|
| install GREG / connect / permissions | developer-mode CLI + launchd agents: IMPLEMENTED; packaged installer: PROPOSED |
| give it goals | signed missions + templates: INTEGRATION TESTED |
| walk away; let it work | supervised persistent body: INTEGRATION TESTED (Linux); MAC-VERIFIED: no |
| **check it from the phone** | device keys + `greg serve` + phone client: INTEGRATION TESTED in real Chromium (iPhone profile); iOS Safari: no |
| wake up; inspect; criticize | morning tribunal + signed critique: UNIT TESTED |
| operate the computer | fs, CLI, git, **browser (real Chromium)**, macOS notify/screenshot/frontmost: LOCAL-REAL for browser; Mac adapters unverified |
| build missing tools | Capability Genesis with frozen oracles: UNIT TESTED; coding-agent builder route: PROPOSED |
| businesses, hires, hardware | PROPOSED (compute recommendation mechanism UNIT TESTED) |

## 5. Intent conflict matrix

| Original | Later clarification | Effect | Current implementation | Correction |
|---|---|---|---|---|
| "build the metaphors literally" | INTENT-0030 | real running effects, not simulations | greg/ built as mechanisms | none; founder confirmation requested |
| survival | no right to survive (§33) | continuity for missions | exit 0 never restarted; STOP always wins | none |
| self-directed | intelligence never creates authority | selection inside signed scope | light cones; device kinds exclude missions | none |
| preserve everything | Spider-Web kill clause | stop executable waste, keep evidence | metabolism dispositions PROPOSED | founder ratification |
| phone as a body? | phone is a remote interface (§9) | phone never holds state or authority | device keys are delegations; body stays the sole writer | built |
| #112 "usable local loop" | #113 GREG body | one canonical persistent runtime | two lineages open | port #112's fixes into #113; #112 kept as benchmark |
| #70 vs #87 runtime spine | 09-11 sequencing | one authority path | greg/ uses neither | founder decision |

## 6. Repository role map

Kernel: authority, proof, memory, the GREG body. DALEOBANKS: media, audience and community organ
(#79 community correction). WMI: opportunity and venture organ. RailScout: named organ slot, no
code. PumpStation: historical crypto coordination, not delegable. RESEARCH-IN: source-ingestion
candidate. build-your-own-x: mechanism atlas only.

## 7. Open-PR reconciliation (verified 2026-09-26)

| PR | Decision |
|---|---|
| #113 (this) | MERGE CANDIDATE after founder review; carries #101/#102/#110, #108/#109, #111 |
| #112 | PORT: resume-authority fix, integration-v1 audit, no lazy fetch (done); console kept as BENCHMARK; then CLOSE AFTER MIGRATION (founder) |
| #70 | PORT the verified-deficit facts (done); runtime ownership NEEDS FOUNDER DECISION |
| #101, #102, #108, #109, #110, #111 | SUPERSEDE by #113 (their commits are preserved inside it) |
| #105 provider-change intake | PRESERVE AS EXPERIMENT; feeds the future model router |
| #93 GoalChase sandbox | PRESERVE AS EXPERIMENT (sealed) |
| #44, #52, #53, #68 | PRESERVE (plans and research); no runtime role |
| DALEOBANKS #79 | PRESERVE; its rule is the candidate gate for community influence in greg/dataplane.py |

## 8. What was misinterpreted

1. A well-formed hash treated as authority to resume standing cognition (a defect on `main`).
2. The #112 console's CSRF token presented as a usable founder loop (it states it is synthetic).
3. The earlier tests' restart-before-approve pattern implied "approval works". It worked only
   after a restart.
4. "The browser can only reach the target" was first implemented as DNS-only. That was false for
   IP literals.

## 9. Retained

Every canonical Kernel owner; the greg/ body; all PR lineages (history preserved); GoalChase as a
sealed experiment; #112's console as a benchmark.

## 10. Regressed

The bare-hash resume path (now refused, and ignored on replay); the DNS-only egress claim.

## 11. Reclassified

#112 console → C (competing, benchmark); #70 deficit rule → B (extracted); DALEOBANKS #79 → D
(complementary).

## 12. Superseded

The legacy bare-hash resume records remain history, never authority.

## 13. Product definition

GREG: a persistent personal artificial institution, a governed operating layer above macOS.
Face: CLI now, phone web client now, native shell later. Body: supervised runtime. Mind: missions,
routing, genesis. Law: the Kernel.

## 14. First body / Mac architecture

launchd agents (`ai.uniimente.greg.body` with KeepAlive `SuccessfulExit=false`;
`ai.uniimente.greg.remote` with KeepAlive true). Python runtime. Keychain-backed secret handles.
`sandbox-exec` isolation. osascript/screencapture adapters. The installed Chrome drives
`browser.render`. Later: a signed .app registering the same agents through SMAppService, with a
native Swift face that consumes the Kernel and never becomes a second kernel. The verification
package is `greg/mac/verify_mac_body.sh` (now also browser + remote channel). NOT MAC-VERIFIED.

## 15. Phone / remote architecture

Built this session.
- The phone runs a web client with a non-extractable WebCrypto Ed25519 key in IndexedDB.
- The founder key delegates it via `DEVICE_ENROLL`, with kinds limited to decide, critique,
  lifecycle, pause, resume and stop, and at most 90 days.
- The phone reaches the Mac over HTTPS on the tailnet (`tailscale serve`).
- `greg serve` is loopback-only transport: signed reads, commands pre-verified, then handed to
  the inbox.
- The body re-verifies every command and records the signer.
- INTEGRATION TESTED in Chromium with an iPhone profile; iOS Safari unverified.

## 16. Persistent runtime architecture

One writer per body. Signed-file inbox. Tick loop that wakes on inbox. Exit-code contract (0 stays
stopped). Boot recovery record. Uncertain dispatch is reconciled, never blindly retried. **Fixed:**
the mission book is rebuilt before applying commands.

## 17. Infinite Goal Chase architecture

Event-sourced missions; discrepancy → highest-value strategy (learned routing) → Gate action →
re-observation → setpoint ladder / homeostasis. **New:** NO_STRATEGY missions keep watching the
world, withdraw moot escalations, and re-escalate on recurrence. The integration watch runs on the
real repositories.

## 18. Computer-use architecture

Route priority: API → app integration → CLI → browser → OS automation → visual.
- Built: fs, git, CLI isolation, `browser.render` (real Chromium, target-bound, origin-only
  egress), and macOS notify/screenshot/frontmost.
- Missing: Accessibility-driven UI control, keyboard/pointer, a visual model.

## 19. Capability Genesis architecture

Search ladder (attached → verified detached → installed software → builder → founder) with frozen
oracles and isolated independent verification. **New:** each deficit records three facts
(required, failed, unserviceable), a mechanism taken from #70. The coding-agent builder route is
PROPOSED.

## 20. AI workforce architecture

PROPOSED. There is no model router yet (zero model calls). It needs provider keys, which are a
founder decision; #105 provides the change-intake governance.

## 21. SOP / automation compounding

`sop.py` proposes SOPs from repeated verified procedures, and the founder ratifies them. Routing
knowledge compounds across missions. There is no economic compounding evidence.

## 22. Human workforce architecture

PROPOSED. Humans are principals with rights and refusal, never tools.

## 23. Business operator architecture

PROPOSED. It comes after VEPMC=1. The first loop uses the DALEOBANKS and WMI organs through Kernel
contracts.

## 24. Zero-trust / cryptographic proof

- Ed25519 founder envelopes; device delegations; signed reads.
- Hash-chained single-writer ledger.
- Single-use effect-bound grants; witness signatures.
- Separate-process appraiser (re-verifies signatures, re-derives from receipts, re-observes the
  world).
- A verified resume that requires a Gate grant.

## 25. Prompt-injection / hostile-input model

- External content goes to the untrusted data plane: it may inform, never command or grant.
- Browser output is labeled `untrusted-external-data`, and page scripts cannot reach third
  parties.
- Commands that fail verification become data-only rejections.

## 26. Economic / treasury model

`capital/treasury.py` is historical design. Financial and irreversible effects are never
delegable to a scope. No money is handled.

## 27. Hardware / compute growth model

`compute.py`: telemetry → sustained bottleneck → one recommendation with alternatives → founder
decision → `NODE_ENROLL` with a bounded node identity. No purchase authority.

## 28. Autonomy graduation model

Per-capability light cones and exact-scope approvals. `autonomy/levels.py` (evidence-earned,
falls immediately) is the next extraction. Device keys never widen scope.

## 29. Security / recovery / shutdown

- Ways to stop: local STOP file, signed stop from the founder or the phone, SIGTERM, supervisor
  bootout.
- Exit 0 is never restarted.
- The resume bypass is closed.
- Revocation takes effect immediately at the server (read-only view per request) and in the body.

## 30. Single bottleneck metric

VEPMC = **0**. On Linux, 8 of 9 conditions are demonstrated; `mac_body` requires Alfonso's Mac,
and `founder_accepted` requires his own key.

## 31. First vertical closure

The §24 acceptance path via the CLI (`test_greg_vepmc_path.py`), and now the same approval
boundary and stop via the phone (`test_greg_phone_e2e.py`).

## 32. Tests executed

- Full suite: 819 passed.
- GREG unit tests under the seccomp launcher: 74 passed.
- Phone end-to-end: 6 of 6 after the fix.
- VEPMC path and supervised tests, re-recorded.
- `browser.render`: 3 tests.
- Mutation checks: JS canonicalization; dead-proxy egress; routing (earlier).
- The Mac-package blocks were executed on Linux.

## 33. CI results

The last pushed head `2a71189` was green on all four Canonical CI jobs. This session's push is
pending CI at the time of writing.

## 34. Negative evidence

See `tests/evidence/greg-body/README.md`. Four defects were reproduced before being fixed, three of
them already pushed. My first deterministic test was a false pass. Three weak test controls were
caught on self-review.

## 35. Current limitations

- Not MAC-VERIFIED or reboot-verified.
- iOS Safari unverified.
- Founder key custody is a file on the Mac, not the Secure Enclave.
- The phone page is served by the Mac it controls.
- As root on Linux, Chromium runs unsandboxed.
- No model router, coding-agent builder, business loop or human fabric.
- Spine refresh is O(n).

## 36. Exact implementation changes

- `egregore/runtime.py`: resume via the Gate (from #112).
- `egregore/repository_audit.py`: integration-v1 profile (from #112).
- `greg/capabilities.py`: `repo.integration_audit`, `browser.render`, `target_from`,
  `target-host-only`.
- `greg/authority.py`: target-host binding.
- `greg/missions.py`: watch-while-blocked, withdrawal, recurrence epochs, withdrawn answers
  refused.
- `greg/body.py`: rebuild before commands, device enroll/revoke, signer recorded, devices in
  status.
- `greg/founder.py`: device delegation, signed reads.
- `greg/remote.py`, `greg/phone/`: the phone channel.
- `greg/cli.py`: device and serve commands.
- `greg/service.py`: `--remote` agents.
- `greg/genesis.py`: verified-deficit facts.
- `greg/templates.py`: integration-watch.
- `greg/tribunal.py`: withdrawn requests closed.
- `greg/mac/verify_mac_body.sh`: browser + remote checks.
- Tests: `test_greg_remote.py`, `test_greg_browser.py`, `test_greg_resume_authority.py`,
  `test_greg_phone_e2e.py`, plus additions to `test_greg_spiderweb.py`, `test_greg_missions.py`,
  `test_greg_platform.py` and `test_egregore_runtime.py`.
- Docs: runbook, README, decision §14, metabolism, evidence.

## 37. Next executable step

Alfonso runs `greg/FIRST_MISSION.md` on his Mac: install, enroll his key, submit the two missions,
kill -9, approve (optionally from his phone per step 5b), accept, and confirm `greg vepmc` → 1.
Then, in this order:
1. Extract `evolution/repair`'s blind function-loss detector into Genesis restore.
2. Connect a coding-agent builder route behind frozen oracles (milestone 3).
3. Build the model router under #105's governance.

## 38. Founder decisions required

1. Merge #113 (and close #112 after its fixes are confirmed ported)?
2. Is "build the metaphors literally" satisfied by real running effect mechanisms?
3. Ratify or amend the A–G metabolism dispositions.
4. Runtime ownership: #70 vs #87 (greg/ uses neither).
5. Tailscale as the phone transport, or another authenticated TLS proxy?
6. Which model provider(s) and keys for the model router?
7. Which first business loop to graduate after VEPMC = 1?
