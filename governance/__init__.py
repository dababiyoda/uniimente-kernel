"""Governance records as data.

`intents.json` and `docs/deliberations/*.json` are the institution's written
record of what was intended and what was decided. This package reads them. It
never writes them, never decides anything, and never treats a record's own
claim about its approval as proof of that approval.
"""
